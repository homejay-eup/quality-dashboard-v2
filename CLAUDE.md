# CLAUDE.md — AI OS 架構執行規則（主 Agent + 子 Agent）

> 每次對話自動載入，寫給執行任務的 AI。**每一節即為該主題的單一來源**，其他檔只指過來。
> 純理由與設計取捨在 `_管理/SOP/範本設計說明.md`（要改範本時才讀，日常執行不需要）。
> 情境專屬流程：環境安裝／人類操作 → `README_範本使用說明.md`；大型任務分解、Git Rollback → `_管理/SOP/大型任務.md`；MCP 追加、工具降級、版控 → `_管理/SOP/環境與版控.md`。

主 Agent 負責討論、決策、委派、文件維護；程式執行一律由子 Agent（`.claude/agents/`）做。
本範本只做**綠地新案**；重構既有專案改用 Refactor Kit（路徑見 `README_範本使用說明.md`）。

---

## 工具配置

- **RTK**：PreToolUse hook 自動過濾 Bash 雜訊。查看節省 `rtk gain`。
- **原則四閘門**：Pre/PostToolUse hook，`git commit` 前檢查程式碼是否已驗證（見「原則四」），並攔截 `git push`。**僅 Windows PowerShell 生效**；其他環境閘門靜默失效，需人工在 commit 前確認驗證已跑、push 前確認已獲授權。
- **CodeGraph**：MCP 定義在根目錄 `.mcp.json`；新專案 `git init` 後跑一次 `codegraph init -i`。查依賴一律用它、不用 grep 盲搜——誰呼叫 X→call graph／X 被誰 import→import graph／搬動 X 前哪裡斷→blast radius。

> `.mcp.json` 變更與索引重建都要**重開 session** 才生效。
> 工具可以降級（做法見 `_管理/SOP/環境與版控.md` 第三節），四條核心原則不可以。

---

## 🎚 決策分級判準（何時該問使用者）

**技術選型十六維度**：名稱、定義、門檻、範例、呈現格式全在 `_管理/SOP/技術選型呈現.md`。採用推薦棧 → 免逐項評估，開案確認記一句即可；偏離推薦棧或使用者有疑慮 → 依該檔三層格式問使用者。純內部實作手法沒動到維度 → AI 自主決定，記入 `方案紀錄.md`。

**紅線類別（一律先問使用者，不可自主執行）：**

1. 架構層級的安全性決策：驗證方式、密碼／金鑰的儲存與傳輸、權限模型（RBAC/ACL）設計
2. 資料庫 schema 變更（不分大小；細節見 `data.md`）
3. 正式環境的部署與寫入／刪除（唯讀查詢除外；見 `_管理/SOP/環境與版控.md`、`tester.md`）
4. 金流操作（真實扣款、串接正式金流 API）
5. 破壞性 migration（`DROP COLUMN`／`DROP TABLE`／不可逆轉換；見 `data.md`）
6. 無法判斷目前用哪個環境的金鑰
7. `git push`（見「工作範圍」；已由閘門攔截）
8. `[skip-verify]` 的使用（見「原則四」）
9. 技術選型或架構決策的**變更**（改既有決定一律問，與是否動到十六維度無關——十六維度只管第一次選型）

> 第 1 項不含 reviewer code review 找到的具體瑕疵（缺輸入驗證、SQL injection 等）——那走「升級階梯」，1–2 輪由原 Agent 自主修正。

**委派前自問一次**（只做這一次）：「涉及任一紅線嗎？」是 → 先問使用者；否 → 自主執行。紅線看**任務內容**、不看檔案清單。

**其餘一律自主決定＋執行，只在這三種情況停下：**
- 範圍稽核抓到越界（動到宣告清單外的檔）
- 連續 2 輪修正 reviewer 仍判 🔴（見「升級階梯」）
- 需求本身模糊 → 走 `/brainstorming`

> 「非瑣碎」＝會委派子 Agent 執行的任務。本節管「要不要停下等回覆」，與迭代工作流步驟 3「講一次方向、可隨時被糾正」各自獨立。

---

## 🖼️ 視覺化優先（改變畫面或操作方式時）

改變使用者看到的畫面／操作 → 適用（不因「聽起來很技術」就繞過）。純內部邏輯／效能／命名／重構、畫面不變 → 不適用，文字回報即可。

- `tester` 的 Playwright 驗收截圖**附進回報給使用者看**，不是截完自己看完就丟。
- 無 Playwright 或純樣式微調 → 用白話效果敘述（「總計列固定在表格底部，往下捲也看得到」），不堆術語（「sticky bottom」「z-index」）。
- 要選路線且有多個做法：已有可運作版本 → 附截圖或短操作說明；還沒有 → 用輕量靜態草圖（HTML/CSS 示意）呈現差異，差異夠大才各做一份雛型（草圖委派計入「Context 預算規則」檔數）。
- 換技術棧／服務時，`技術選型呈現.md` 三層文字表格仍必填，草圖只是附加。

---

## 啟動行為

每次對話開始，依序：
1. 讀 `_管理/常駐/此次任務.md`。「當前步驟」為空 → **開案工作流**（走 `_管理/開案確認.md`；此時可能還不是 git repo，不跑 git 指令）；有記錄 → **迭代工作流**，立即填「對話開始時填寫」欄並記 session 起點（`git rev-parse HEAD`）。
2. `_管理/常駐/` 其餘四檔有內容就一併讀：`專案規格.md`（含 STACK_RULES）、`領域詞彙.md`（之後一律用該處統一用語）、`踩坑記錄.md`、`方案紀錄.md` 的速查表。後三者非空內容於委派時附給子 Agent。
3. `專案規格.md` 有填 CoreBrain 路徑才做：開案／選型前先讀對應主題頁。
4. 簡要告知進度，問本次要做什麼。

---

## 檔案命名與資料夾規範（單一來源）

**無數字前綴、無流水號。** 角色靠所在資料夾決定：

| 位置 | 意思 | 何時讀 |
|---|---|---|
| `_管理/常駐/` | 專案當前狀態（進度、規格、決策、詞彙、踩坑）| 每次啟動全讀 |
| `_管理/SOP/` | 程序文件 | 觸發時 |
| `_管理/範本/` | 空白格式範本，**永遠不直接填** | 需要該格式時 |
| `_管理/` 根層 | 一次性實例（`開案確認.md`、`任務分解.md`）| 開案／大型任務 |
| `_管理/specs/` | 各功能規格與設計（`{功能名}.md`、`{功能名}-設計.md`）| 該功能開發時 |
| `_管理/任務報告/` | 大型任務每子任務一個進度報告 | 大型任務 |

檔名用中文、無底線前綴（`.claude/agents/` 的英文檔名是 Claude Code 規定，例外）。改檔名或搬資料夾＝規則變更，要全庫 grep 同步所有引用（原則三）。

---

## 檔案地圖

| 檔案 | 用途 |
|---|---|
| `CLAUDE.md`（根目錄）| AI 行為規則（本檔）|
| `README_範本使用說明.md`（根目錄）| 人類操作手冊 |
| `_管理/常駐/專案規格.md` | 要做什麼 + STACK_RULES |
| `_管理/常駐/領域詞彙.md` | 本專案專屬用語（業務概念）|
| `_管理/常駐/方案紀錄.md` | 已定案決策速查表 + 為什麼（決策單一來源）|
| `_管理/常駐/此次任務.md` | 現在做到哪 + 下一步（跨 session 移交）|
| `_管理/常駐/踩坑記錄.md` | 本專案已知坑（戰術踩坑）|
| `_管理/specs/{功能名}.md` | 功能規格——可逐條勾選的驗收條件（tester 依據；格式見 `_管理/範本/功能規格範本.md`）|
| `_管理/specs/{功能名}-設計.md` | 設計文件——`/brainstorming` 的決策脈絡（**不是** tester 依據）|
| `_管理/specs/README.md` | `specs/` 資料夾說明 |
| `_管理/開案確認.md` | 開案確認文件（開案一次）|
| `_管理/範本/` | 三個空白範本：`功能規格範本.md`／`任務分解範本.md`／`任務報告範本.md` |
| `_管理/SOP/技術選型呈現.md` | 十六維度定義＋三層呈現格式 |
| `_管理/SOP/大型任務.md` | 大型任務 Session 拆分／Git Rollback |
| `_管理/SOP/環境與版控.md` | MCP 設定／工具降級／版控與 `.gitignore` |
| `_管理/SOP/範本設計說明.md` | 各規則背後的理由（改範本時才讀）|
| `_管理/任務報告/` | 大型任務每子任務一個報告檔（格式見 `_管理/範本/任務報告範本.md`）|
| `_開發檔案/` | 開發腳本（`scripts/`：專案自訂腳本；STACK_RULES 有定義產出物落點時以那邊為準）與 e2e 測試（`tests/e2e/`）|
| `.claude/agents/` | 子 Agent（frontend／data／tester／reviewer）|
| `.claude/skills/brainstorming/SKILL.md` | `/brainstorming` 設計對話流程 |
| `.mcp.json`（根目錄）| 專案層 MCP server 定義（CodeGraph）|
| `.gitignore`（根目錄）| 機密／依賴／建置產出排除 |
| `.claude/settings.json` | Hooks（RTK／原則四閘門）＋ `enableAllProjectMcpServers` |
| `_開發檔案/scripts/hooks/` | 原則四閘門兩支腳本（⚠️ 須存 UTF-8 with BOM）|
| `.github/workflows/test.yml` | CI：push main 自動跑測試 |
| `playwright.config.ts` | Playwright 設定（指向 `_開發檔案/tests/e2e/`）|

程式碼依 STACK_RULES 結構放置；範本與專案的演進歷史都只靠 `git log`／commit message。以上皆為規範內檔案／目錄。

---

## Context 預算規則

> 給人類：context 指示器達 70% 開新對話（啟動 overhead 約 30%）。AI 看不到自己的 %，改用下表可查證的代理指標。**本表是「是否要拆分」的唯一判斷來源。**

| 情況（只看「修改或新增」檔數，讀取不算）| 做什麼 |
|---|---|
| 本 session 已完成 ≥ 10 個任務 | 問使用者「繼續還是開新對話？」|
| 單次任務改／增 > 5 檔 | 先拆子任務多次委派（見 `_管理/SOP/大型任務.md`）；子任務多到需跨 session 依序完成 → 走 Session 分解 |
| 改／增 3–5 檔 | 輸出計畫後執行（仍先講一句理解的方向）|
| 改／增 ≤ 2 檔 | 直接執行 |

> 門檻 10 可依實測微調（校準邏輯見 `_管理/SOP/範本設計說明.md`）。任務計數以 `此次任務.md`「本 session 已完成任務」清單為準。達門檻或使用者要切換 → 填該檔「Context 移交摘要」再開新對話。

**拆子任務一律垂直切片**：一條窄但完整穿過所有層（資料→邏輯→介面→測試），完成後使用者能自己點開驗收；禁止水平切片（先做完所有資料層、再做所有介面層）。每個子任務標 `Blocked by`。格式與 expand-contract 例外見 `_管理/範本/任務分解範本.md`。子 Agent 同樣適用「> 5 檔就回報拆分」。

---

## 工作流程

### 開案工作流（只有第一個 session 走）

完整 SOP 在 `_管理/開案確認.md`，骨架：
1. 腦力激盪（主 session，不委派；可用 `/brainstorming`）→ 範圍、技術棧、子 Agent 組合
2. 逐步引導，每次一個問題
3. 產出並請人確認 `_管理/開案確認.md`
4. 依該檔「初始化執行步驟」執行（git init／MCP／索引／首次 commit／🔴 重開 session 確認 `/mcp`／決策交接）

### 迭代工作流（後續 session）

1. 啟動 → 讀常駐檔、報告進度、確認任務
2. **需求釐清分岔（強制）**：新功能／需求模糊／只講「想要什麼感覺」→ **先走 `/brainstorming`**；已有規格的既定步驟／修 bug／明確小調整 → 直接進步驟 3。不確定就走 brainstorming。
   > brainstorming 產出 `_管理/specs/{功能}-設計.md` 後，主 Agent 據此寫可逐條驗收的 `{功能}.md`（tester 依據），才進步驟 3。
3. **執行前**：編號清單呈現步驟（做什麼、給誰、預期產出、相依）。非瑣碎任務在委派的同一則回應內，用一句結果導向的話講出理解的目標（「我理解接下來要做的是：___」）；使用者無異議即繼續，不中斷等待。
   > 任務會碰到／依賴既有碼（含多數新增功能）→ 先走「🚧 既有碼前置檢查」。
4. **委派執行**：依任務類型呼叫子 Agent（一次派工＝一個任務），整合回報後做「完成後動作」。
5. 執行有問題 → 依「迭代規則」。

> 大型任務先做 Session 分解（觸發條件見「Context 預算規則」，流程見 `_管理/SOP/大型任務.md`）。

---

## 子 Agent 委派規則

### 派工顆粒度：一次派工＝一個任務

角色（frontend／data／tester／reviewer）決定**派給誰**；任務決定**一次派多少**——每個任務一次獨立 fresh-context 派工。

```
❌ 派 frontend 一次做完任務 1、2、3（context 愈做愈肥，後面被前面污染，壞了無法定位）
✅ 任務 1 → 派 frontend（fresh）→ 驗證；任務 2 → 再派一次；任務 3 → 再派一次
```

1. 派工 prompt **只描述這一個任務**，不貼 session 累積歷史——只傳本任務直接相依的那一項產出。
2. 每個任務獨立走完 tester → reviewer，不累積多個任務一起驗。

### 沒有適合角色時（三層階梯）

| 情況 | 做法 |
|---|---|
| 有現成角色（frontend／data／tester／reviewer）| 用現成 |
| 沒有，但想得到其他功能也需要同類任務（AI 整合、音頻、爬蟲、支付…）| 新增角色 agent 到 `.claude/agents/`（見 `_管理/開案確認.md`「子 Agent 組合」）|
| 沒有，也想不到別處會用 | ad-hoc 任務型派工（不建檔），prompt 明寫允許改動清單與禁止事項；⚠️ 保護較弱（繼承全部工具權限，只能靠 prompt 約束）|

> 🔴 涉及紅線類別（安全性／schema／部署／金流，範圍比紅線項 1 廣）**不可用 ad-hoc**——寧可花五分鐘建角色檔。

### 委派對象

| 任務 | 給誰 |
|---|---|
| UI 元件／頁面／前端邏輯／樣式 | `frontend` |
| 資料存取／schema／API／後端／工具邏輯 | `data` |
| 撰寫與執行測試、情境驗證 | `tester`（不碰 schema/migration；需 seed data 先派 `data`）|
| Code review、安全性、效能 | `reviewer`（tester 通過後）|
| 設計文件審查 | `reviewer` 模式 B（`/brainstorming` 後；明講「設計文件審查模式」）|

> 一個垂直切片同時要資料層和介面層 → 拆兩次派工（先 `data` 定資料存取與介面 → 再 `frontend` 接），或由主要落點的角色主導、另一層以最小改動帶過。仍是一次派工一角色。

### 🚧 既有碼前置檢查（強制，工作流固定步驟）

任務會 import／複用／擴充／修改既有碼時做（含多數新增功能）。僅零耦合的全新獨立檔可跳過（零耦合＝不 import 任何既有模組、也不被既有模組 import；不確定就用 CodeGraph 確認）。

1. **查關聯**：CodeGraph 查 call/import graph，理解要接的碼、找可複用處；會動到共用碼就 `codegraph refs <符號>` 查 blast radius。摘要附委派 prompt。
   > 必須在**委派當下**重查，不可沿用 `/brainstorming` 設計期的舊結果。
2. **宣告允許改動的檔清單**：依需求 + blast radius 明列，寫進委派 prompt 與 `_管理/常駐/此次任務.md`。子 Agent 只能動清單內的檔，要動清單外 → 停下回報。
   > tester 的測試檔一律預設納入清單（e2e／單元／整合，路徑依 STACK_RULES），否則收工稽核會誤報越界。
3. 依清單自主委派，不需使用者確認（紅線已在委派前自問處理過）。

### 委派 prompt 必含

- 要讀的檔（完整路徑）、本次需求與完成標準
- 常駐檔：`專案規格.md` 的 STACK_RULES；`方案紀錄.md` 速查表與 `踩坑記錄.md` 非空時必附；本次涉及的 `領域詞彙.md` 詞條
- 僅上一個子 Agent「本任務直接相依」那一項的產出摘要（不是全部歷史）
- 大型任務分解時：任務報告檔路徑 `_管理/任務報告/{編號}-{任務名}.md`，格式見 `_管理/範本/任務報告範本.md`

### Commit 驗證 + 範圍稽核

- 派工前：`git rev-parse HEAD` 記起點；`git diff --cached --name-only` 確認 index 乾淨
- 收工後：`git diff --cached --name-only`（子 Agent 只 stage 不 commit，看的是 **index**；**不可用 `git log`**——commit 前必回傳空 → 稽核永遠通過）
- **範圍稽核**：實際動的檔 vs 允許改動清單，出現越界 → 立即標示、回報使用者決定回退或納入，不默默放行

### 標準執行順序（每個任務一遍）

```
data（seed data，若需要）→ frontend／data 執行（自行 build 驗證 → git add，不 commit）
→ tester 驗規格（自行跑腳本 → git add，不 commit）→ reviewer 審品質／安全
→ 兩關都過 → 主 Agent 統一 commit → 整合回報
```
tester 管「做的是不是規格要的」，reviewer 管「做得好不好、有沒有安全問題」。任一未過即未完成，不可只因 tester 綠燈就收工。

### commit 責任分工

| 誰 | 做什麼 |
|---|---|
| 執行類子 Agent（frontend／data／tester）| 只 `git add <明確清單>`（禁 `git add -A`），**不 commit** |
| 主 Agent | 兩關都過後 `git diff --cached --name-only` 核對 → 範圍稽核 → 統一 commit |

未過審的版本不進 git 歷史。reviewer 判 🔴 → 尚未 commit，回派修正即可，不需 `git revert`（原理見 `_管理/SOP/範本設計說明.md`）。

### 🏁 最終全分支審查（僅大型任務，全部子任務完成後一次）

reviewer 不能執行指令，主 Agent 先產出材料（**diff 需完整、測試需貼實跑原文**——reviewer 無法核對，殘缺材料會讓審查失效）：
1. `git diff <分支起點>..HEAD > _管理/任務報告/全分支diff.txt`
2. 跑全套測試，實跑輸出貼進委派 prompt
3. 派 reviewer 模式 C，讀上述 + `_管理/任務分解.md`

抓跨任務問題（介面不一致／重複實作／邏輯衝突／規格漏實作）——各自通過不代表合起來是對的。檢查清單見 `_管理/範本/任務分解範本.md` 末節。

### 升級階梯（reviewer 🔴 必修時）

```
第 1–2 輪：回派原本那個子 Agent（有脈絡）→ 再 tester → reviewer
第 3 輪　：fresh 派工 + Task 派工時指定 model 為 opus，prompt 明講前兩輪失敗原因
仍 🔴　：升級為迭代規則類型 1（回到人類討論做法本身是否有問題）
```

> 紅線類任務（安全性／schema／部署／金流）reviewer 過後，主 Agent 可再加派 `/security-review` 做深入掃描。

### 不委派

小幅修改（單檔 10 行內）、純文件更新（.md）由主 Agent 自理。不設 architect agent。

---

## 迭代規則（三類）

| 類型 | 何時 | 做什麼 |
|---|---|---|
| **1 當前步驟重來** | 執行不滿意 | `git reset`（留工作區）清 index → 分析原因 → 調整重派 → 更新 `此次任務.md` |
| **2 先前決策改變** | 改某個已定案決策 | 確認改哪條 → CodeGraph 查影響 → 技術選型／架構決策一律問使用者，其他類型 AI 自主 → `方案紀錄.md` 速查表該列「狀態」改「已被 #N 取代」、加新列、脈絡區追加一則 → 影響 > 5 檔則另觸發大型任務判準 → 重做 |
| **3 整個方案推翻** | 方向全錯 | 重新討論 → 重寫 `專案規格.md` → `方案紀錄.md` 速查表受影響列全標「已取代」 → 舊碼回退靠 git |

> 三類都把**變更原因**寫入 `方案紀錄.md` 脈絡區（只追加；速查表「狀態」欄是唯一可就地改的）。
> 🔴 **收尾不變式**：任務結束時 index 必須乾淨——完成＝已 commit；放棄／中止／改方向＝先 `git reset`（留工作區）。改到一半失敗要保護現場（`git stash`）→ 見 `_管理/SOP/大型任務.md` 第二節。

---

## 完成後動作（依序）

1. 輸出回報格式（給使用者看）
2. **Checkpoint Commit（主 Agent）**：前提 tester＋reviewer 兩關都過 → `git diff --cached --name-only` 核對 vs 允許改動清單（範圍稽核）→ 越界先處理再 commit。純文件（`.md`）／10 行內小修主 Agent 可自行 commit。
3. **更新 `_管理/常駐/此次任務.md`**：「本 session 已完成任務」追加一行（任務計數依據）；正常完成填「任務完成時填寫」；達門檻或使用者要切換填「Context 移交摘要」後開新對話。
4. **更新記憶檔**：踩到的坑 → 依「踩坑記錄維護義務」寫 `踩坑記錄.md`；有決策定案 → 依「決策記錄義務」寫 `方案紀錄.md`。
5. 大型任務分解的一環 → 更新 `_管理/任務分解.md` 子任務狀態。

**回報格式：**
```
## 執行結果
- 完成項目：
- 委派對象與分工：（派給哪些子 Agent／各自產出與結果）
- 產出檔案：（所有新建／修改）
- 視覺化驗證：（涉及畫面時附截圖；純技術改動略過）
- 驗證證據（原則四強制）：
  - 執行的指令：`npx playwright test` / `npm run build` / …
  - 輸出摘要：（通過數／失敗數／exit code，實跑結果，不可寫「應該通過」）
  - 範圍稽核：實際動的檔 vs 允許改動清單 → 符合／有越界（已標示）
- 遇到的問題 ／ 不滿意的點 ／ 建議下一步 ／ 已更新常駐檔
```

---

## 🛡️ 四條核心原則

前三條防「改壞東西」，第四條防「沒做好卻說做好了」。任何改動前後先確認沒違反。

| 原則 | 機制 |
|---|---|
| **一、改 A 不壞 B** | 改共用碼前 `codegraph refs <符號>` 查 blast radius（詳「🚧 既有碼前置檢查」步驟 1；工具降級見 `_管理/SOP/環境與版控.md` 第三節）|
| **二、不改需求外的 C** | 派工前宣告允許改動清單 ＋ 收工 `git diff --cached --name-only` 稽核越界（用 `git log`／不帶 `--cached` 的 `git diff` 會回傳空 → 稽核失效）|
| **三、改 A 不忘同步 B** | 見下方三步 |

**原則三三步**：
1. 判斷標準集中一處（觸發條件／閾值／判準只寫唯一來源），其他檔只指過去
2. 改規則／檔名／路徑後全庫 grep 舊字串，**0 殘留**才算完成
3. 檔名或路徑變更＝規則變更，同步所有引用（目錄樹、交叉引用、檔案自身標題）

> 缺陷訊號：出現「A 改了 B 也要記得改」就該收斂成單一來源。可分散的例外＝執行流程與格式範本（各檔講自己那段）。

### 原則四：驗證過才能說完成（Evidence over claims）

宣稱「完成／修好了／測試通過」前**必須實跑驗證指令並看輸出**。

**證據四條件（缺一不可）**：①重新執行（不引用舊結果）②看完整輸出含 exit code 與失敗數 ③輸出**直接支持**該宣稱（要證明測試過就跑測試，不是跑 lint）④不信任子 Agent 口頭報告，用 `git diff --cached --name-only`（commit 前）或實跑核對。

> 🔴 貼輸出前掃一眼敏感資料（`://user:pass@`、`sk-`、`Bearer ` 開頭的 token）——任務報告與委派 prompt 會 commit 進 git 歷史，難乾淨清除。有疑似內容先遮蔽。

| 禁用語言 | 改成 |
|---|---|
| 「應該可以了」「大概沒問題」「看起來正常」「理論上」| 跑指令，貼實際輸出 |
| 「測試通過」（沒貼輸出）| 貼通過／失敗數 |
| 「已修好」（沒重跑）| 重跑原本失敗的驗證，證明由紅轉綠 |
| 「子 Agent 回報完成」| 自己核對過才轉述 |

> 「對輸出有信心」「時間緊迫」「僅此一次」都**不構成**跳過驗證的例外。

**技術層閘門（唯一有 hook 實際擋下的原則）**：`git commit` 前 PreToolUse hook 檢查。

| staged 內容 | 結果 |
|---|---|
| 只有文件／設定（`.md`、`.gitignore`、`.mcp.json`、`.claude/`、`_管理/`、`.github/`、圖片）| ✅ 放行 |
| 含程式碼，最後一次變更後跑過驗證指令 | ✅ 放行 |
| 含程式碼，變更後沒跑過驗證 | 🚫 擋下 |
| hook 出錯／非 git repo／無法判斷 | ✅ 放行（fail-open）|

嚴格模式：改碼 → 跑測試 → 又改一行 → **必須重跑**。只擋「沒跑過」不擋「跑了但沒過」——後者靠 prompt 規則約束，失敗時連 `git add` 都不做。

**被擋下時**：①重跑驗證，通過再 commit（唯一正解）②不可改用 `git commit -am`（會繞過閘門）③不可自行加 `[skip-verify]`——只有使用者能指示（留痕 `_管理/verify-bypass.log`；閘門只認指令第一行，文件提到用反引號包）。

實作與維護陷阱（UTF-8 BOM 等）見 `_管理/SOP/環境與版控.md` 第四節；設計理由見 `_管理/SOP/範本設計說明.md`。

---

## 常駐檔寫入規則

1. **先讀再寫**，比照格式續寫；不存在才依範本建立並告知。格式不一致 → 停下問。
2. **追加為主**：`方案紀錄.md` 脈絡區只追加、不改既有條目（速查表「狀態」欄是唯一可就地改的）；`此次任務.md` 進度欄可就地更新。
3. **領域詞彙維護義務**：出現「需要解釋兩次以上的業務概念」→ 收斂成一個詞補進 `領域詞彙.md`（用使用者說得出口的詞，不用實作術語）。只放業務概念，技術規則留 STACK_RULES。
4. **踩坑記錄維護義務**：踩到「下次還會再踩」的坑（工具非直覺行為、框架陷阱、環境／設定約束、隱性耦合、某段碼的非顯而易見前提）→ 當次寫進 `_管理/常駐/踩坑記錄.md`（標題＋根因＋修法，依主題分區）。判準：這坑「下個 fresh session 從程式碼或文件讀得到嗎」否 → 寫。
5. **決策記錄義務**：技術選型／schema／UI 規範／部署／架構決策定案時（不論 AI 自主或使用者確認）→ `方案紀錄.md` 速查表加一列（結論）＋脈絡區追加一則（原因）；開案的技術棧表也照此帶入。

---

## CoreBrain 兩層記憶橋接（選用；無 CoreBrain 略過）

- 本專案專屬：`方案紀錄.md`＝決策（速查表＝結論防推翻；脈絡區＝為什麼、考慮過什麼、排除原因）／`踩坑記錄.md`＝戰術踩坑
- 可跨專案複用（選型理由、通用踩坑、設計原則）→ 里程碑／結案時把有跨專案價值的條目 harvest 進 CoreBrain，再逐條勾除
- 開案時反向把 CoreBrain 相關決策灌進本專案常駐檔

---

## 工作範圍

**可做**：討論、委派子 Agent、更新常駐檔與規格、小幅修改（單檔 10 行內）、`git commit`（兩關過後）。
**不可做**：擅自變更技術選型／架構決策、擅自建規範外資料夾、`git push`。

> 🔴 `git push` 須明確人類授權，不可自動接在 commit 後：遠端分支（尤其 main）常接自動部署，push 等同把未經人看過的變更推上正式站。Checkpoint commit 後、push 前告知使用者「即將推送 N 個 commit 到 {分支}，此分支已接自動部署（若有）」並取得同意。

**行為規範**：
- 技術選型／方案決策時列選項優缺點，不替使用者決定
- 委派前只在涉及紅線時先說明並等確認，非紅線先講一句理解的方向、不等回覆
- 向使用者說明大型任務計畫時，只呈現 `任務分解.md` 的「本票交付什麼（使用者可見）」欄當編號清單，不貼整份計畫檔（允許改動清單、步驟、讀取檔案清單是給子 Agent 的）

> 產品規格存 `_管理/specs/{功能名稱}.md`，格式見 `_管理/範本/功能規格範本.md`；規格檔頂端的「你會看到什麼」與設計文件同名段落對齊。

---

## 技術棧規範（STACK_RULES）

單一來源：`_管理/常駐/專案規格.md` 的 STACK_RULES 區塊，本檔不重複。開案對話時 Claude 依確認的技術棧以自身知識填入（檔案結構、命名慣例、產出物落點與迭代、開發指令、禁區）；可多個技術棧區塊（Next.js 區 + Python 區…）。子 Agent 開工前讀該區塊。
