# 此次任務

> **啟動行為判定為「迭代工作流」時立即填寫下方第一區**（開案工作流走 `_管理/開案確認.md`，不填此區）。
> Session 中斷也有紀錄可循；Session 結束時（無論正常完成或 context 截斷）**必須更新下方對應區塊**。

---

## 對話開始時填寫（迭代工作流必填）

- 目前專案：設備品質分析儀表板 v2（quality-dashboard-v2）
- **本 session 起點 commit**：d30ecd1992ef8dedb0f547dd21a682ef407d113c
- 當前步驟編號與名稱：專案現況盤點＋Python 產出路徑決策
- 本次要做的事：①`codegraph init -i` 建索引 ②向使用者白話重新介紹整個專案（用途/資料流/工作流程/規則邏輯/架構）③核對專案內容與線上網站是否一致 ④依使用者決定，把 Python 產出路徑降級為參考用
- 相依的前步驟產出：上個 session 完成「在線平均已使用年限」功能；查證後發現該功能與 push hook 移除的 commit 其實已經在 GitHub `master` 上（只有 push hook 那筆文件 commit 尚未推送）
- 完成標準：使用者理解專案全貌；確認 repo 與線上網站一致；Python 產出路徑決策落地到常駐檔與規則文件
- 需要讀取的相關檔案：`_管理/常駐/專案規格.md`、`_管理/常駐/領域詞彙.md`、`設備品質分析_分冊文件/00_規則定義.md`、`index.html`

---

## 本 session 已完成任務（**逐筆追加，不覆寫**）

> 這是「任務計數」的唯一依據（見 `CLAUDE.md` Context 預算規則）。每完成一個任務就追加一行；達 **10 個以上**主動詢問是否開新對話（門檻數字的單一來源是 `CLAUDE.md`「Context 預算規則」表，若該表調整以那邊為準）。

| # | 任務名稱 | commit hash |
|---|---|---|
| 1 | 專案搬家：從 GitHub `master` 重新 clone、套用 AI-OS 範本骨架、補回規則文件與 Python 腳本、重寫 `_管理/常駐/` 四檔 | 0680a0e |
| 2 | 補進「Google Sheet 資料來源 vs BI 資料庫」核對記錄（摘自另一專案）| c28ce6b |
| 3 | 補規則文件對照指標註解、修正 `transform.js`／`README.md` 過時版本號 | b2469b1 |
| 4 | 移除使用者確認不需要的 `README.md`／`README_範本使用說明.md`（保留 `index.html`）| 701c19f |
| 5 | `/brainstorming`：「在線平均已使用年限」設計文件（含 reviewer 審查與補正）| efc05f6, 2f72cc4（文件部分）|
| 6 | 產出功能規格 `在線平均已使用年限.md` | 48be851 |
| 7 | 委派 frontend 實作核心功能（OAuth 授權/讀資料/算年限/KPI卡）＋獨立 tester／reviewer 驗證 | 2f72cc4（程式碼部分，混入意外的大範圍 commit，細節見踩坑記錄）|
| 8 | 委派 frontend 實作快照擴充（匯出快照時也存這份新資料）＋獨立 tester 驗證 | 2f72cc4（程式碼部分，同上）|
| 9 | reviewer 建議修正落地（背景換發補 catch、私有 Sheet ID 集中管理）| d0f998c |
| 10 | 建 codegraph 索引；向使用者白話重新介紹專案並核對 repo／線上網站一致性；依使用者決定將 Python 產出路徑降級為參考用（更新常駐檔＋規則文件現況說明）| e8b99d8 |
| 11 | 排查「在線平均已使用年限」功能網頁上看不到的問題，找出根因是 GitHub Pages 自 8/6 起沒有成功重新建置（不是瀏覽器快取），加 `.nojekyll` 修復；使用者授權後 push，確認網站已更新到最新內容 | 1767448 |
| 12 | KPI 卡片依車機/鏡頭分頁各自只顯示對應的「在線平均已使用年限」卡片 | ab4a5e9 |
| 13 | 「比率表」「分析表」新增「在線平均已使用年限(年)」欄位（委派 frontend → 獨立 tester 21 案例驗證 → reviewer 無🔴）| 154fe9c |

---

## 任務完成時填寫（正常結束，必填）

- **完成了什麼**：
  1. 專案搬家（見任務1）：從 GitHub `master` 重新 clone，套用 AI-OS 範本骨架，補回規則文件與 Python 腳本，重寫常駐檔
  2. 搬家後的收尾清理：補進跨專案摘錄的資料來源核對記錄、修正規則文件對照指標註解與過時版本號、依使用者要求移除不需要的 README 文件
  3. 新功能「在線平均已使用年限」（`總線上量`也能看平均已使用年限）：完整走過 `/brainstorming` → reviewer 審設計文件 → 功能規格 → 委派 frontend（拆兩個獨立子任務：核心 KPI 卡＋快照擴充）→ 兩者各自獨立 tester 驗證 → reviewer 審程式碼品質/安全性 → 依建議補小修正。功能本身：改用 OAuth（擴增現有 Google 登入的 Sheets 讀取範圍）讀取私有 Sheet「車機鏡頭上線明細」，算出車機/鏡頭各自的在線平均已使用年限（規則 H，已寫入 `00_規則定義.md` v10），KPI 卡失敗只影響自己不拖累其他畫面；匯出快照時這份資料也一併存進共用雲端硬碟
- **下一步**：無預排步驟。可選的後續延伸（都還沒問過使用者是否要做）：
  ①確認是否要 `git push`（會直接影響 GitHub Pages 線上網站，需使用者明確授權；目前本地領先 origin/master 多個 commit，無分岔可直進）
  ②Google Cloud 端要確認：這個 OAuth Client（`js/auth.js` 的 `CLIENT_ID`）是否已啟用 Google Sheets API、consent screen 是否已設定 `spreadsheets.readonly` scope——這步驟在 Google Cloud Console 做，不是程式碼範圍，需要使用者或有管理權限的人確認過才能讓新功能真正在線上生效
  ③本機舊資料夾（`品質分析\` 與其巢狀 `新版儀表板\`、`quality-analysis\`）是否要保留或清理，使用者尚未決定
  ④「去年同期」比較畫面本次刻意不做（快照才剛開始存，資料還沒累積），累積夠幾季後可以再開一輪 `/brainstorming` 討論怎麼呈現
- **待決定事項**：是否要 push；Google Cloud OAuth 設定是否已就位；舊本機資料夾去留
- **已知問題**：見 `踩坑記錄.md`「git commit 誤把多個並行任務的 staged 檔案一起提交」——已修復（本 session 已妥善驗證+補commit），但下次多個 agent 並行 `git add` 時要記得 commit 時限定路徑

---

## Context 移交摘要（需切換 session 時使用）

- **專案名稱**：設備品質分析儀表板 v2（quality-dashboard-v2）
- **目前走到第幾步**：搬家已完成並 push 前待授權；「在線平均已使用年限」功能已實作完成並通過 tester/reviewer，本地已 commit，尚未 push
- **本次完成了什麼**：見上方「任務完成時填寫」
- **尚未完成的事（下一步）**：`git push` 授權；Google Cloud OAuth／Sheets API 設定確認；舊資料夾去留
- **相關檔案路徑**：`_管理/常駐/方案紀錄.md`、`_管理/常駐/踩坑記錄.md`、`_管理/specs/在線平均已使用年限.md`、`_管理/specs/在線平均已使用年限-設計.md`
- **待決定事項**：是否 push；OAuth Client 的 Google Cloud 設定；舊資料夾去留
- **已知問題 / 注意事項**：無（`踩坑記錄.md` 已記錄本 session 的流程疏失與修法）
