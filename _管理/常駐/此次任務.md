# 此次任務

> **啟動行為判定為「迭代工作流」時立即填寫下方第一區**（開案工作流走 `_管理/開案確認.md`，不填此區）。
> Session 中斷也有紀錄可循；Session 結束時（無論正常完成或 context 截斷）**必須更新下方對應區塊**。

---

## 對話開始時填寫（迭代工作流必填）

- 目前專案：設備品質分析儀表板 v2（quality-dashboard-v2）
- **本 session 起點 commit**：d92f7a2b8084281a970aad71142a0d4915d2e4c8（clone 自 GitHub `master` 時的 HEAD）
- 當前步驟編號與名稱：專案搬家（非開案，套用既有 AI-OS 範本於既有 repo）
- 本次要做的事：把使用者原「品質分析」AI-OS 資料夾的範本骨架搬到以 GitHub `quality-dashboard-v2` 為基準的新資料夾，並補回本機唯一持有的規則文件與 Python 腳本，讓專案能在新家繼續用同套流程開發
- 相依的前步驟產出：無（沿用範本，非任務分解的一環）
- 完成標準：新資料夾程式碼與 GitHub `master`／線上網站一致；規則文件與 Python 腳本補齊且進版控；`_管理/常駐/` 四檔重新針對本專案撰寫完成
- 需要讀取的相關檔案：
  - `_管理/常駐/方案紀錄.md`（搬家決策脈絡）
  - `_管理/常駐/踩坑記錄.md`（本機多份 clone 落後、規則文件版本落差）
  - `設備品質分析_分冊文件/00_規則定義.md`（規則 A–F，現行 v9）

---

## 本 session 已完成任務（**逐筆追加，不覆寫**）

> 這是「任務計數」的唯一依據（見 `CLAUDE.md` Context 預算規則）。每完成一個任務就追加一行；達 **10 個以上**主動詢問是否開新對話（門檻數字的單一來源是 `CLAUDE.md`「Context 預算規則」表，若該表調整以那邊為準）。

| # | 任務名稱 | commit hash |
|---|---|---|
| 1 | 專案搬家：從 GitHub `master` 重新 clone、套用 AI-OS 範本骨架、補回規則文件與 Python 腳本、重寫 `_管理/常駐/` 四檔 | （待本次 commit）|

---

## 任務完成時填寫（正常結束，必填）

- **完成了什麼**：
  1. 確認 GitHub `quality-dashboard-v2` 的 `master` 是 GitHub Pages 實際部署來源（legacy branch 部署，無 Actions），且比本機兩份既有 clone（`品質分析\` 落後 60 commit、`品質分析\新版儀表板\` 落後 50 commit）都新
  2. 從 GitHub 重新 `git clone` 到 `C:\Users\EupUser\Desktop\quality-dashboard-v2`
  3. 複製 AI-OS 範本骨架：`CLAUDE.md`、`README_範本使用說明.md`、`.mcp.json`、`.claude/agents`、`.claude/skills`、`.claude/settings.json`、`_管理/範本`、`_管理/SOP`（`.claude/memory` 不帶，重新累積）
  4. 從本機唯一存放處 `C:\Users\EupUser\Desktop\品質分析\` 補回 `設備品質分析_分冊文件/`（規則定義全套）與 `新版產出/`（Python 批次腳本＋交接文件，排除 3 個 xlsx 快照與 `__pycache__`）
  5. 重新撰寫 `_管理/常駐/` 四檔（專案規格／領域詞彙／方案紀錄／踩坑記錄），內容針對本專案（設備品質分析儀表板），不沿用舊「品質分析」資料夾（CRM 資料工作站）的業務內容
- **下一步**：無預排步驟。可選的後續延伸（都還沒問過使用者是否要做）：①修正 `README.md` 裡「規則 A–F（v5）」與實際版本（v9）不一致的地方；② commit 這次搬家的變更；③確認是否要 `git push`（會直接影響 GitHub Pages 線上網站，需使用者明確授權）；④本機舊資料夾（`品質分析\` 與其巢狀 `新版儀表板\`、`quality-analysis\`）是否要保留或清理，使用者尚未決定
- **待決定事項**：是否要 push 這次的變更；舊本機資料夾要保留多久／要不要清理
- **已知問題**：無

---

## Context 移交摘要（需切換 session 時使用）

- **專案名稱**：設備品質分析儀表板 v2（quality-dashboard-v2）
- **目前走到第幾步**：專案搬家已完成本地端整理，尚未 commit／push
- **本次完成了什麼**：見上方「任務完成時填寫」
- **尚未完成的事（下一步）**：commit＋（待授權後）push；README 版本號小修正
- **相關檔案路徑**：`_管理/常駐/方案紀錄.md`、`_管理/常駐/踩坑記錄.md`、`設備品質分析_分冊文件/00_規則定義.md`
- **待決定事項**：是否 push；舊資料夾去留
- **已知問題 / 注意事項**：無
